// Generated by protoc-gen-jsonarray. DO NOT EDIT.
// Source: address.proto

'use strict';

class Address {
  /**
   * @constructor
   */
  constructor() {
    /** @type {string} */
    this.street = "";
    /** @type {string} */
    this.city = "";
    /** @type {string} */
    this.state = "";
    /** @type {number} */
    this.zip = 0;
  }

  /**
   * @returns {string}
   */
  getStreet() {
    return this.street;
  }

  /**
   * @param {string} value
   */
  setStreet(value) {
    this.street = value;
  }

  /**
   * @returns {string}
   */
  getCity() {
    return this.city;
  }

  /**
   * @param {string} value
   */
  setCity(value) {
    this.city = value;
  }

  /**
   * @returns {string}
   */
  getState() {
    return this.state;
  }

  /**
   * @param {string} value
   */
  setState(value) {
    this.state = value;
  }

  /**
   * @returns {number}
   */
  getZip() {
    return this.zip;
  }

  /**
   * @param {number} value
   */
  setZip(value) {
    this.zip = value;
  }

  /**
   * Serialize this message to a positional JSON array.
   * @returns {Array} The serialized array.
   */
  serialize() {
    const array = [];
    array.push(this.street);
    array.push(this.city);
    array.push(this.state);
    array.push(this.zip);
    return array;
  }

  /**
   * Serialize this message to a JSON-compatible array (used by JSON.stringify).
   * @returns {Array} The serialized array.
   */
  toJSON() {
    return this.serialize();
  }

  /**
   * Serialize this message to a JSON string.
   * @returns {string} The JSON string.
   */
  toJsonString() {
    return JSON.stringify(this.serialize());
  }

  /**
   * Deserialize from a positional JSON array or JSON string.
   * @param {Array|string} data - The array or JSON string to deserialize from.
   * @returns {Address} The deserialized message.
   */
  static deserialize(data) {
    const array = typeof data === 'string' ? JSON.parse(data) : data;
    const obj = new Address();
    const size = array.length;
    if (size > 0 && array[0] != null) {
      obj.setStreet(String(array[0]));
    }
    if (size > 1 && array[1] != null) {
      obj.setCity(String(array[1]));
    }
    if (size > 2 && array[2] != null) {
      obj.setState(String(array[2]));
    }
    if (size > 3 && array[3] != null) {
      obj.setZip(Number(array[3]));
    }
    return obj;
  }

  /**
   * Deserialize from a JSON string.
   * @param {string} json - The JSON string.
   * @returns {Address} The deserialized message.
   */
  static fromJsonString(json) {
    return Address.deserialize(JSON.parse(json));
  }

  /**
   * @returns {string}
   */
  toString() {
    return `Address{street=${this.street}, city=${this.city}, state=${this.state}, zip=${this.zip}}`;
  }
}

module.exports = { Address };
